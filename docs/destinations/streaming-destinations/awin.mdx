---
title: "Awin"
description: Step-by-step guide on sending your event data from RudderStack to Awin.
---

[Awin](https://www.Awin.com/) is an interactive email marketing tool. It provides several useful features like sending transactional campaigns, email automation, and drip emails with customer journeys, WYSIWYG email editor, and many other tool integrations.

<div class="infoBlock">
Find the open source code for this destination in the <a href="https://github.com/rudderlabs/rudder-transformer/tree/master/v0/destinations/mailjet">GitHub repository</a>.
</div>

## Getting started

Before configuring Awin as a destination in RudderStack, verify if the source platform is supported by Awin by referring to the table below:

| Connection Mode | Web | Mobile | Server |
| :--- | :--- | :--- | :--- |
| Device mode | - | - | - |
| Cloud mode | Supported  | Supported | Supported |

<div class="infoBlock">
To know more about the difference between cloud mode and device mode in RudderStack, refer to the <Link to="/destinations/rudderstack-connection-modes/">RudderStack Connection Modes</Link> guide.
</div>

Once you have confirmed that the source platform supports sending events to Awin, follow these steps:

1. From your [RudderStack dashboard](https://app.rudderstack.com/), add a source. Then, from the list of destinations, select **Awin**.
2. Assign a name to the destination and click **Continue**.

## Connection settings

To successfully configure Awin as a destination, you will need to configure the following settings:

<img src="../../assets/event-stream-destinations/Awin-connection-settings.png" alt="Awin connection settings" />

- **Advertiser ID**: Enter your Awin API key.

<div class="infoBlock">
For more information on obtaining your Awin API key, refer to the <Link to="#FAQ">FAQ</Link> section below.
</div>

- **Client-side Events Filtering**: This field is **only applicable** for the `identify` call. Enter the name of the Awin list where the contacts should be added. If not provided, `Rudderstack` is taken as the default list name. 

## Track

You can use the <Link to="/event-spec/standard-events/track">`track`</Link> call to [add custom events for a contact](https://www.Awin.com/developers/93cba3fa7f1ea-add-event/).

A sample `track` call is shown below:

```javascript
{
  "userId": "identified user id",
  "anonymousId": "anon-id-new",
  "event": "Product Purchased new",
  "properties": {
    "name": "Shirt",
    "revenue": 4.99,
    "voucherCode": "FIRSTSALE",
    "currency": "USD",
    "commissionGroup": "CD"
  },
  "context": {
    "ip": "14.5.67.21",
    "library": {
      "name": "http"
    }
  },
  "timestamp": "2020-02-02T00:23:09.544Z"
}
```

### Supported mappings

The following table lists the mappings between the RudderStack and Awin properties:

| RudderStack property | Awin property | Notes |
| :--------------| :---------| :--------------|
| `properties.revenue` <br/> `properties.totalAmount` | `totalAmount` |  |
| `properties.commissionGroup` | `commissionGroup` |  |
| `properties.voucherCode` | `ts` | `voucher_code` | |
| `properties.currency` <br/> `properties.currencyCode` | `currencyCode` | |
| `order_id` | `orderReference` | |

## FAQ

### Where can I find the Awin advertiser ID?

To obtain your Awin advertiser ID, follow these steps:

1. Log into your [Awin dashboard](https://manage.Awin.com/auth/login).
2. Go to **Settings** > **API Keys** as shown:

<img src="../../assets/event-stream-destinations/Awin-api-key.png" alt="Awin API key" />
