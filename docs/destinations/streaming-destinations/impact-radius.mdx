---
title: "Impact"
description: Send your event data from RudderStack to Impact.
---

[Impact](https://impact.com/) is a global partnership management platform that helps in managing and scaling different partnerships, such as affiliates, influencers, strategic business partners, mobile apps, commerce content publishers, B2B, and more.

<div class="infoBlock">
Find the open source transformer code for this destination in the <a href="https://github.com/rudderlabs/rudder-transformer/tree/master/src/v0/destinations/impact_radius">GitHub repository</a>.
</div>

## Getting started

RudderStack supports sending events to Impact via the following <a href="https://rudderstack.com/docs/rudderstack-cloud/rudderstack-connection-modes/">connection modes</a>:

| **Connection Mode** | **Web** | **Mobile** | **Server** |
| :--- | :--- | :--- | :--- |
| **Device mode** | -  | - | - |
| **Cloud mode** | **Supported**  | **Supported** | **Supported** |

Once you have confirmed that the source platform supports sending events to Impact, follow these steps:

1. From your [RudderStack dashboard](https://app.rudderstack.com/), add a source. Then, from the list of destinations, select **Impact**.
2. Assign a name to the destination and click **Continue**.

## Connection settings

To successfully configure Impact as a destination, you will need to configure the following settings:

- **Account SID**: Enter the integration key from PagerDuty service. Refer to the <a href="#faq">FAQ</a> section for more information on how to obtain it.
- **API Key**: dedup_key is an id of an event. You can provide this key. Otherwise, PagerDuty generates a dedup_key and assigns it to an event. This key is used to update, resolve, or acknowledge an event.
- **Campaign Id**: Enter the integration key from PagerDuty service. Refer to the <a href="#faq">FAQ</a> section for more information on how to obtain it.
- **Impact App Id**: dedup_key is an id of an event. You can provide this key. Otherwise, PagerDuty generates a dedup_key and assigns 
- **Event Type Id**: Enter the integration key from PagerDuty service. Refer to the <a href="#faq">FAQ</a> section for more information on how to obtain it.
- **Enable to hash email**: dedup_key is an id of an event. You can provide this key. Otherwise, PagerDuty generates a dedup_key and assigns 
- **Additional parameters mapping**: Enter the integration key from PagerDuty service. Refer to the <a href="#faq">FAQ</a> section for more information on how to obtain it.
- **Custom mapping for Products**: dedup_key is an id of an event. You can provide this key. Otherwise, PagerDuty generates a dedup_key and assigns 
- **Enable Identify Events**: dedup_key is an id of an event. You can provide this key. Otherwise, PagerDuty generates a dedup_key and assigns 
- **Enable Page Events**: Enter the integration key from PagerDuty service. Refer to the <a href="#faq">FAQ</a> section for more information on how to obtain it.
- **Enable Screen Events**: dedup_key is an id of an event. You can provide this key. Otherwise, PagerDuty generates a dedup_key and assigns 
- **Action Event Names**: Enter the integration key from PagerDuty service. Refer to the <a href="#faq">FAQ</a> section for more information on how to obtain it.
- **Install Event Names**: dedup_key is an id of an event. You can provide this key. Otherwise, PagerDuty generates a dedup_key and assigns 

## Identify

You can send an <Link to="/event-spec/standard-events/identify">`identify`</Link> call to impact.com via the [`PageLoad` endpoint](https://integrations.impact.com/impact-brand/reference/create-a-pageload) which lets impact.com update the user identifiers for an accurate correlation.

A sample `identify` call is shown below:

```javascript
rudderanalytics.identify("1hKOmRA4el9Zt1WSfVJIVo4GRlm", {
  email: "alex@example.com",
});
```

### Property mappings

The following table lists the mappings between RudderStack and Impact properties:

| RudderStack property | Impact property | 
| :----------------- | :--------------- |
|`context.device.id`|`AppleIfv`|
|`context.app.name`|`AppName`|
|`context.app.build`|`AppPackage`|
|`context.app.version`|`AppVer`|
|`settings.campaignId`|`CampaignId`|
|`context.traits.email` <br/> `properties.email`|`CustomerEmail`|
|`userId`|`CustomerId`|
|`anonymousId`|`CustomProfileId`|
|`context.network.carrier`|`DeviceCarrier`|
|`context.locale`|`DeviceLocale`|
|`context.device.manufacturer`|`DeviceMfr`|
|`context.device.model`|`DeviceModel`|
|`context.device.type` <br/> `context.os.name`|`DeviceOs`|
|`context.os.version`|`DeviceOsVer`|
|`timestamp`|`EventDate`|
|`context.device.id`|`GoogAId`|
|`settings.appId` <br/> `context.app.namespace`|`ImpactAppId`|
|`context.ip`|`IpAddress`|
|`context.page.url` <br/> `properties.url` <br/> `context.page.referrer` <br/> `context.referrer.url`|`PageUrl`|
|`context.referrer.url` <br/> `context.page.referrer`|`ReferringUrl`|
|`context.userAgent`|`UserAgent`|
|`context.device.advertisingId`|`AppleIfa`|

## Track

Track calls send event data to impact.com via the Conversions(reported as conversions) endpoint or the PageLoad(reported as a Click) endpoint

You can send a <Link to="/event-spec/standard-events/track">`track`</Link> call to impact.com to send event data via the [`Conversions` endpoint](https://integrations.impact.com/impact-brand/reference/submit-a-conversion) or [`PageLoad` endpoint](https://integrations.impact.com/impact-brand/reference/create-a-pageload).

A sample `track` call is shown below:

```javascript
rudderanalytics.track("Order Completed", {
  order_id: "1234",
  shipping: 22,
  tax: 1,
  discount: 1.5,
  coupon: "ImagePro",
  currency: "USD",
  products: [{
      sku: "G-32",
      name: "Monopoly",
      price: 14,
      quantity: 1,
      category: "Games",
      brand: "top"
    },
    {
      sku: "G-34",
      name: "Monopoly",
      price: 16,
      quantity: 3,
      category: "Games",
      brand: "top"
    },
  ],
})
```

Email address will be SHA1 hashed (with a HEX output
type) prior to being sent to impact.com. The email input is expected to
be plain text (e.g. example@company.com).

#### Property mappings

The following table lists the mappings between RudderStack and Impact properties for change events:

| RudderStack property | Impact property | 
| :----------------- | :--------------- |
|`context.device.adTrackingEnabled`|`AppleAdTrack`|
|`context.device.advertisingId`|`AppleIfa`|
|`context.device.id`|`AppleIfv`|
|`context.app.name`|`AppName`|
|`context.app.namespace`|`AppPackage`|
|`context.app.version`|`AppVer`|
|`settings.campaignId`|`CampaignId`|
|`context.referrer.id|| properties.clickId`|`ClickId`|
|`properties.currency`|`CurrencyCode`|
|`context.traits.email` <br/> `properties.email`|`CustomerEmail[1]`|
|`userId`|`CustomerId`|
|`context.traits.status`|`CustomerStatus`|
|`anonymousId`|`CustomProfileId`|
|`context.network.carrier`|`DeviceCarrier`|
|`context.locale`|`DeviceLocale`|
|`context.device.manufacturer`|`DeviceMfr`|
|`context.device.model`|`DeviceModel`|
|`context.device.type` <br/> `context.os.name`|`DeviceOs`|
|`context.os.version`|`DeviceOsVer`|
|`INSTALL`|`EventCode`|
|`timestamp`|`EventDate`|
|`event`|`EventTypeCode`|
|`settings.eventTypeId`|`EventTypeId`|
|`properties.advertisingId`|`GoogAId`|
|`context.ip`|`IpAddress`|
|`properties.products[i].brand`|`ItemBrand{i}`|
|`properties.products[i].category`|`ItemCategory{i}`|
|`properties.products[i].name`|`ItemName{i}`|
|`properties.products[i].price`|`ItemPrice{i}`|
|`properties.products[i].coupon`|`ItemPromoCode{i}`|
|`properties.products[i].quantity`|`ItemQuantity{i}`|
|`properties.products[i].sku`|`ItemSku{i}`|
|`context.location.latitude`|`Latitude`|
|`context.location.longitude`|`Longitude`|
|`properties.discount`|`OrderDiscount`|
|`properties.orderId` <br/> `properties.order_id` <br/> `properties.transactionID` <br/> `properties.checkout_id`|`OrderId`|
|`properties.coupon` <br/> `properties.coupon_id`|`OrderPromoCode`|
|`properties.shipping`|`OrderShipping`|
|`properties.tax`|`OrderTax`|
|`context.referrer.url` <br/> `context.page.referrer`|`ReferringUrl`|
|`context.page.url` <br/> `properties.url` <br/> `context.page.referrer` <br/> `context.referrer.url`|`PageUrl`|
|`context.userAgent`|`UserAgent`|

## Page

You can send a <Link to="/event-spec/standard-events/page">`page`</Link> call to impact.com via the [`PageLoad` endpoint](https://integrations.impact.com/impact-brand/reference/create-a-pageload).

A sample `page` call is as shown:

#### Property mappings

The following table lists the mappings between RudderStack and Impact properties for change events:

| RudderStack property | Impact property | 
| :----------------- | :--------------- |
|`context.device.id`|`AppleIfv`|
|`context.app.name`|`AppName`|
|`context.app.build`|`AppPackage`|
|`context.app.version`|`AppVer`|
|`settings.campaignId`|`CampaignId`|
|`context.traits.email` <br/> `properties.email`|`CustomerEmail`|
|`userId`|`CustomerId`|
|`anonymousId`|`CustomProfileId`|
|`context.network.carrier`|`DeviceCarrier`|
|`context.locale`|`DeviceLocale`|
|`context.device.manufacturer`|`DeviceMfr`|
|`context.device.model`|`DeviceModel`|
|`context.device.type` <br/> `context.os.name`|`DeviceOs`|
|`context.os.version`|`DeviceOsVer`|
|`timestamp`|`EventDate`|
|`context.device.id`|`GoogAId`|
|`settings.appId` <br/> `context.app.namespace`|`ImpactAppId`|
|`context.ip`|`IpAddress`|
|`context.page.url` <br/> `properties.url` <br/> `context.page.referrer` <br/> `context.referrer.url`|`PageUrl`|
|`context.referrer.url` <br/> `context.page.referrer`|`ReferringUrl`|
|`context.userAgent`|`UserAgent`|
|`context.device.advertisingId`|`AppleIfa`|

## Screen

You can send a <Link to="/event-spec/standard-events/screen">`screen`</Link> call to impact.com via the [`PageLoad` endpoint](https://integrations.impact.com/impact-brand/reference/create-a-pageload). It acts as the mobile equivalent of a `page` call.

A sample `screen` call is as shown:

#### Property mappings

The following table lists the mappings between RudderStack and Impact properties for change events:

| RudderStack property | Impact property | 
| :----------------- | :--------------- |
|`context.device.id`|`AppleIfv`|
|`context.app.name`|`AppName`|
|`context.app.build`|`AppPackage`|
|`context.app.version`|`AppVer`|
|`settings.campaignId`|`CampaignId`|
|`context.traits.email` <br/> `properties.email`|`CustomerEmail`|
|`userId`|`CustomerId`|
|`anonymousId`|`CustomProfileId`|
|`context.network.carrier`|`DeviceCarrier`|
|`context.locale`|`DeviceLocale`|
|`context.device.manufacturer`|`DeviceMfr`|
|`context.device.model`|`DeviceModel`|
|`context.device.type` <br/> `context.os.name`|`DeviceOs`|
|`context.os.version`|`DeviceOsVer`|
|`timestamp`|`EventDate`|
|`context.device.id`|`GoogAId`|
|`settings.appId` <br/> `context.app.namespace`|`ImpactAppId`|
|`context.ip`|`IpAddress`|
|`context.page.url` <br/> `properties.url` <br/> `context.page.referrer` <br/> `context.referrer.url`|`PageUrl`|
|`context.referrer.url` <br/> `context.page.referrer`|`ReferringUrl`|
|`context.userAgent`|`UserAgent`|
|`context.device.advertisingId`|`AppleIfa`|

## FAQ

### Where can I find the Impact Routing Key?

1. Log in to the [Impact dashboard](https://app.Impact.com/).
2. Go to **Services** > **Service Directory**.
3. Select your desired service and click **Integrations** tab.
4. Click **Add integration** and select any desired integration platform. Here, you can find the integration/routing key as shown:

<img src="../../assets/event-stream-destinations/integration-key-Impact.png" alt="Impact integration key"/>


### How can I debug errors?

Any error in impact.com may arise due to failed pathing, invalid parameters, authentication errors, and network unavailability. Refer to the [Errors documentation](https://integrations.impact.com/impact-brand/reference/errors). Additionally, you can also refer to the [Rate Limits documentation](https://integrations.impact.com/impact-brand/reference/rate-limits) for more information on the API call limits.