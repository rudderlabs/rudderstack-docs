---
title: "Destination_Name"
description: Step-by-step guide on sending your event data from RudderStack to **Destination_Name**.
aliases: ["dest_name","dest_name_2"]  // These alias values are used to optimize the page our search index, making it easier for users to find the integration in the docs.
---

> **To contributor: Some helpful tips before you get started**:
> - Make sure you go through our guide on [**submitting an integration PR**](https://rudderstack.com/docs/user-guides/how-to-guides/how-to-submit-an-integration-pull-request/) for a quick overview on the things to keep in mind while writing the doc.
> - You can this use this [**destination doc**](https://rudderstack.com/docs/destinations/marketing/klaviyo/) as a reference for writing your instructions.
> - Do not include any screenshots/images. We will include them after merging the changes.
> - Include tips and suggestions wherever possible.


[Destination](Link)- Add some introduction about the destination platform.

<div class="infoBlock">
Find the open source transformer code for this destination in the <a href="https://github.com/rudderlabs/rudder-transformer/tree/master/src/v0/destinations/<dest_name>">GitHub repository</a>.
</div>

## Getting started

RudderStack supports sending events to <Destination_Name> via the following <Link to="/destinations/rudderstack-connection-modes/">connection modes</Link>:

| **Connection Mode** | **Web**       | **Mobile**    | **Server**    |
| :------------------ | :------------ | :------------ | :------------ |
| **Device mode**     |               |               |               |
| **Cloud mode**      |               |               |               |


Once you have confirmed that the source platform supports sending events to <Destination_Name>, follow these steps:

1. From your [RudderStack dashboard](https://app.rudderstack.com/), add the source. Then, from the list of destinations, select **<Destination_Name>**.
2. Assign a name to your destination and click **Continue**.

### Connection settings 

To successfully set up Mixpanel as a destination, you need to configure the following settings:

- **<Destination_details>**: Enter your <Destination_details> here. <Reference to destination doc/FAQ, if useful/applicable>

## Identify

<Link to="/event-spec/standard-events/identify/">`identify`</Link> call
<usage of the call>
A sample `identify` call is shown below:

```javascript
```
  
### Property mappings
  
The following table lists the mappings between the RudderStack and <Destination_Name> properties:

| RudderStack property | <Dest_name> property |
| :------------------- | :---------------- |
|      (required)      |                   |
|                      |                   |
|                      |                   |

  
  
## Track
  
<Link to="/event-spec/standard-events/track/">`track`</Link> call
<usage of the call>
A sample `track` call is shown below:

```javascript
```
  
## Page
  
<Link to="/event-spec/standard-events/page/">`page`</Link> call
<usage of the call>
A sample `page` call is shown below:

```javascript
```
  
## Group
  
<Link to="/event-spec/standard-events/group/">`group`</Link> call
<usage of the call>
A sample `group` call is shown below:

```javascript
```
  
## Screen
  
<Link to="/event-spec/standard-events/screen/">`screen`</Link> call
<usage of the call>
A sample `screen` call is shown below:

```javascript
```

## Common mappings (If any) 
 
The following table lists the common property mappings between the RudderStack and <Destination_Name>:

| RudderStack property | <Dest_name> property |
| :------------------- | :---------------- |
|      (required)      |                   |
|                      |                   |
|                      |                   |
  
## FAQ
